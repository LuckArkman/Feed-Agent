"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const express_1 = require("express");
const DraftController_1 = __importDefault(require("../controllers/DraftController"));
const authMiddleware_1 = require("../middlewares/authMiddleware");
const router = (0, express_1.Router)();
// All draft routes are protected and require a valid user token
router.use(authMiddleware_1.authMiddleware);
/**
 * @openapi
 * /api/drafts:
 *   get:
 *     summary: List all news drafts
 *     description: Retrieve all news drafts generated by OCR+LLM for the authenticated user.
 *     tags: [Drafts]
 *     security:
 *       - bearerAuth: []
 *     parameters:
 *       - in: query
 *         name: status
 *         schema:
 *           type: string
 *           enum: [PENDING, APPROVED, REJECTED]
 *         description: Optional status to filter drafts by.
 *     responses:
 *       200:
 *         description: A list of drafts.
 *       400:
 *         description: Invalid status provided.
 *       401:
 *         description: Unauthorized.
 */
router.get('/', DraftController_1.default.getDrafts.bind(DraftController_1.default));
/**
 * @openapi
 * /api/drafts:
 *   post:
 *     summary: Create a new draft
 *     description: Manually create a news draft.
 *     tags: [Drafts]
 *     security:
 *       - bearerAuth: []
 *     requestBody:
 *       required: true
 *       content:
 *         application/json:
 *           schema:
 *             type: object
 *             properties:
 *               title:
 *                 type: string
 *               summary:
 *                 type: string
 *               content:
 *                 type: string
 *               source:
 *                 type: string
 *               category:
 *                 type: string
 *               priority:
 *                 type: string
 *               status:
 *                 type: string
 *               imagePath:
 *                 type: string
 *     responses:
 *       201:
 *         description: Draft created successfully.
 *       400:
 *         description: Invalid input.
 *       401:
 *         description: Unauthorized.
 */
router.post('/', DraftController_1.default.createDraft.bind(DraftController_1.default));
/**
 * @openapi
 * /api/drafts/{id}:
 *   get:
 *     summary: Get draft details
 *     description: Retrieves details of a specific draft, including the original raw OCR text and the generated structured JSON content.
 *     tags: [Drafts]
 *     security:
 *       - bearerAuth: []
 *     parameters:
 *       - in: path
 *         name: id
 *         required: true
 *         schema:
 *           type: integer
 *         description: The ID of the draft to retrieve.
 *     responses:
 *       200:
 *         description: Draft details retrieved successfully.
 *       400:
 *         description: Invalid draft ID.
 *       401:
 *         description: Unauthorized.
 *       404:
 *         description: Draft not found or does not belong to user.
 */
router.get('/:id', DraftController_1.default.getDraftById.bind(DraftController_1.default));
/**
 * @openapi
 * /api/drafts/{id}:
 *   put:
 *     summary: Update draft content
 *     description: Updates the generated JSON content (titulo, resumo, fonte) of a specific draft. The input is sanitized to prevent XSS.
 *     tags: [Drafts]
 *     security:
 *       - bearerAuth: []
 *     parameters:
 *       - in: path
 *         name: id
 *         required: true
 *         schema:
 *           type: integer
 *         description: The ID of the draft to update.
 *     requestBody:
 *       required: true
 *       content:
 *         application/json:
 *           schema:
 *             type: object
 *             required:
 *               - titulo
 *               - resumo
 *               - fonte
 *             properties:
 *               titulo:
 *                 type: string
 *               resumo:
 *                 type: string
 *               fonte:
 *                 type: string
 *     responses:
 *       200:
 *         description: Draft updated successfully.
 *       400:
 *         description: Invalid input or missing fields.
 *       401:
 *         description: Unauthorized.
 *       404:
 *         description: Draft not found or does not belong to user.
 */
router.put('/:id', DraftController_1.default.updateDraft.bind(DraftController_1.default));
/**
 * @openapi
 * /api/drafts/{id}/approve:
 *   post:
 *     summary: Approve a draft
 *     description: Marks the draft as APPROVED and queues it for bulk broadcasting to all active contacts.
 *     tags: [Drafts]
 *     security:
 *       - bearerAuth: []
 *     parameters:
 *       - in: path
 *         name: id
 *         required: true
 *         schema:
 *           type: integer
 *         description: The ID of the draft to approve.
 *     responses:
 *       200:
 *         description: Draft approved and queued for broadcasting.
 *       400:
 *         description: Invalid draft ID.
 *       401:
 *         description: Unauthorized.
 *       404:
 *         description: Draft not found or does not belong to user.
 */
router.post('/:id/approve', DraftController_1.default.approveDraft.bind(DraftController_1.default));
/**
 * @openapi
 * /api/drafts/{id}/reject:
 *   post:
 *     summary: Reject a draft
 *     description: Marks the draft as REJECTED. It will not be sent.
 *     tags: [Drafts]
 *     security:
 *       - bearerAuth: []
 *     parameters:
 *       - in: path
 *         name: id
 *         required: true
 *         schema:
 *           type: integer
 *         description: The ID of the draft to reject.
 *     responses:
 *       200:
 *         description: Draft rejected successfully.
 *       400:
 *         description: Invalid draft ID.
 *       401:
 *         description: Unauthorized.
 *       404:
 *         description: Draft not found or does not belong to user.
 */
router.post('/:id/reject', DraftController_1.default.rejectDraft.bind(DraftController_1.default));
/**
 * @openapi
 * /api/drafts/{id}/cancel:
 *   post:
 *     summary: Cancel a broadcast
 *     description: Interrupts an ongoing or pending broadcast, updating status to CANCELLED.
 *     tags: [Drafts]
 *     security:
 *       - bearerAuth: []
 *     parameters:
 *       - in: path
 *         name: id
 *         required: true
 *         schema:
 *           type: integer
 *         description: The ID of the draft to cancel.
 *     responses:
 *       200:
 *         description: Broadcast cancelled successfully.
 *       400:
 *         description: Invalid draft ID.
 *       401:
 *         description: Unauthorized.
 *       404:
 *         description: Draft not found or does not belong to user.
 */
router.post('/:id/cancel', DraftController_1.default.cancelDraft.bind(DraftController_1.default));
/**
 * @openapi
 * /api/drafts/broadcast/launch:
 *   post:
 *     summary: Launch a broadcast to specific contacts
 *     description: Enqueues a broadcast job for all APPROVED drafts to the selected contacts.
 *     tags: [Drafts]
 *     security:
 *       - bearerAuth: []
 *     requestBody:
 *       required: true
 *       content:
 *         application/json:
 *           schema:
 *             type: object
 *             required:
 *               - contactIds
 *               - delaySeconds
 *             properties:
 *               contactIds:
 *                 type: array
 *                 items:
 *                   type: integer
 *               delaySeconds:
 *                 type: number
 *     responses:
 *       200:
 *         description: Broadcast launched successfully.
 *       400:
 *         description: Invalid input or missing fields.
 *       401:
 *         description: Unauthorized.
 */
router.post('/broadcast/launch', DraftController_1.default.launchBroadcast.bind(DraftController_1.default));
/**
 * @openapi
 * /api/drafts/{id}:
 *   delete:
 *     summary: Delete a draft
 *     description: Permanently deletes a draft from the database.
 *     tags: [Drafts]
 *     security:
 *       - bearerAuth: []
 *     parameters:
 *       - in: path
 *         name: id
 *         required: true
 *         schema:
 *           type: integer
 *         description: The ID of the draft to delete.
 *     responses:
 *       200:
 *         description: Draft deleted successfully.
 *       400:
 *         description: Invalid draft ID.
 *       401:
 *         description: Unauthorized.
 */
router.delete('/:id', DraftController_1.default.deleteDraft.bind(DraftController_1.default));
exports.default = router;
