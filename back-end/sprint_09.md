### Sprint 9: CRUD de Contatos
- **Objetivo:** Permitir ao Front-end gerenciar contatos.
- **Tarefas:**
  - Endpoint `POST /api/contacts` para cadastro de novo contato.
  - Endpoint `GET /api/contacts` para listagem de contatos do usuÃ¡rio autenticado.
  - Endpoint `PUT /api/contacts/:id` para atualizar dados (ex: desativar envio).
  - Endpoint `DELETE /api/contacts/:id` para remoÃ§Ã£o.
