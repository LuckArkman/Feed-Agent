### Sprint 27: Endpoints de GestÃ£o de Minutas
- **Objetivo:** Permitir ao administrador listar, revisar e ler as minutas.
- **Tarefas:**
  - Endpoint `GET /api/drafts` para listar minutas do usuÃ¡rio.
  - Endpoint `GET /api/drafts/:id` para visualizar detalhes (incluindo o texto OCR original para comparaÃ§Ã£o).
  - Implementar filtros por status (pending/approved).
